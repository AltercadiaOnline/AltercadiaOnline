/**
 * Câmera de exploração ancorada no jogador.
 * Seguidor passivo do protagonista (LERP + clamp) — ver Camera.ts.
 */
export { Camera as PlayerCamera, Camera } from './Camera.js';
export { CameraManager } from './CameraManager.js';
