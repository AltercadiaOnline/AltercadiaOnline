export type UiSurface = 'screen' | 'hud' | 'overlay' | 'render';

/** Camada React oficial — HUD/screen/overlay acima do Phaser. */
export type UiRuntimeMode = 'online-react-v1';

/** Motor de render do mundo — Phaser único (exploração + batalha). */
export type RenderEngine = 'phaser';
