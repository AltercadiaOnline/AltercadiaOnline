export type UiSurface = 'screen' | 'hud' | 'overlay' | 'render';

/** Camada React oficial — HUD/screen/overlay acima do Construct. */
export type UiRuntimeMode = 'online-react-v1';

/** Motor de render do mundo — Construct 3 (exploração). Combate: lógica neste repo. */
export type RenderEngine = 'construct';
