/**
 * Mesa React desativada — Aceitar + mesa 1×1 estão em
 * `src/client/world/playerTradeDomHud.ts` (bundle do jogo),
 * mesmo padrão do duelo casual (snapshot no dist/client).
 */
export function PlayerTradeHud() {
  return null;
}
