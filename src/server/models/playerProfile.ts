import type { PlayerWorldProfile } from '../../shared/world/playerWorldProfile.js';

/** Perfil persistido do jogador no servidor — posição autoritativa de mundo. */
export type PlayerProfile = PlayerWorldProfile;
