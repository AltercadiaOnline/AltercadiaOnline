/** Item soulbound — Diário de Memórias (registro pessoal do jogador). */
export const DIARIO_MEMORIAS_ITEM_ID = 'diario_memorias' as const;

export type SoulboundItemId = typeof DIARIO_MEMORIAS_ITEM_ID;
